# 다음 세션 작업 목록

플레이 테스트에서 나온 것들. 원인은 코드에서 확인해뒀고, 고치는 것만 남았습니다.

## 1. 박격포 쏠 때 렉

**원인**: 포탄 하나가 터질 때마다 `boom()`이 파티클 16개(시즈탱크 26개)를 한 번에 만듭니다.
자주포는 4연장이라 **한 번에 4발 × 16 = 64개**가 같은 프레임에 들어갑니다. 여기에 링, 화면 흔들림,
효과음이 같이 붙습니다.

**고칠 것**
- 파티클을 미리 만들어 둔 풀에서 꺼내 쓰기 (매번 객체를 새로 만들지 않기)
- 연장 사격은 착탄을 흩어서 같은 프레임에 몰리지 않게
- 상한 400개를 화면 밖 판정과 함께 낮추기

## 2. 범위 피해 — 들어가고 있습니다

`updateShells()`에서 착탄 지점 반경 안의 적 전부에게 들어갑니다.
중심 100%, 가장자리 55%.

| 무기 | 반경 |
|---|---|
| 박격포 | 1.20칸 |
| 자주포 | 1.30칸 |
| 시즈탱크 | 1.65칸 |

**문제는 반경이 아니라 보이지 않는 것**입니다. 피해 숫자가 한 마리 것만 뜨니 한 마리만 맞은 것처럼 보입니다.

**고칠 것**
- 착탄 범위 링을 실제 반경과 같은 크기로 (지금은 1.9배라 실제보다 커 보임)
- 광역 피해는 **합계 숫자 하나**로 크게 띄우기 (`x7  340` 식으로 몇 마리 맞았는지)

## 3. 병사 피해가 너무 낮다

지금: 소총병 6 피해 / 0.62초 = **초당 9.7**. 기관총 진지는 초당 45.
자원 15짜리라고는 해도, 자리를 하나 차지하는 값어치가 안 됩니다.

**고칠 것**
- 소총병 피해를 올리고 (초당 20 근처), 참호 위 사격 속도 보너스를 병사에게도
- 대전차병 대형 배율은 유지, 기본 피해만 조정
- 조정 후 `linesim.js`로 3맵 × 2판 돌려서 7~10차 붕괴가 유지되는지 확인

## 4. 1인 배치라 자리 차지가 심하다

지금: 맨땅 1명 / 참호 3명 / 벙커 2명.

**고칠 것**
- 맨땅을 **2명**으로. 참호 4명, 벙커 3명으로 같이 올리기
- 그러면 참호의 값어치(피해 감소)는 그대로 두고 자리 압박만 풀립니다
- 3번(피해 상향)과 같이 조정해야 밸런스가 맞습니다

## 5. 적이 벽을 뚫고 온다

**확인할 것 (아직 원인 미확정)**
- `separate()`의 밀어내기가 적을 벽 칸 안으로 밀어 넣는지 — `walkable()` 되돌림이 있지만
  적이 많으면 한 프레임에 여러 번 밀려 통과할 수 있음
- 벽으로 완전히 막으면 `f[i0] >= 30000`이 되어 `flowS`(구조물 무시)로 넘어가는 규칙이
  의도보다 자주 발동하는지
- 적 상한을 260으로 낮춘 뒤 밀도가 올라가 밀어내기 힘이 세진 것은 아닌지

**고칠 것**
- 밀어낸 뒤 벽 칸이면 되돌리는 판정을 프레임당 1회가 아니라 밀어낼 때마다
- 벽을 부수는 중인 적은 그 자리에 고정

## 6. 수리 AI가 이상하다 — 오늘 넣은 코드의 버그입니다

`crewJob()`이 **판 전체에서 가장 상한 것 하나**를 고릅니다. 그래서
- 공병이 몇 명이든 **전부 같은 건물로 갑니다**
- 목표를 정할 때 아군 진영인지 보지 않아서, 앞줄 벽이 상하면 **직선으로 걸어가 벽 너머**에 섭니다
  (`stepTo`는 길찾기가 아니라 직선 이동입니다)

**고칠 것**
- 공병마다 담당을 나누기 — 이미 다른 공병이 가고 있는 대상은 제외
- 목표를 **자기 줄보다 뒤쪽 또는 같은 줄**로 제한, 그리고 벽 칸 위로는 못 서게
- 도착 판정을 "그 칸"이 아니라 "수리 사거리 안"으로 (벽 앞에 서면 됨)
- 위생병도 같은 문제이므로 같이 수정

## 순서

3번과 4번은 같이 (밸런스가 붙어 있음) → 6번 (오늘 넣은 버그) → 5번 (원인 조사부터) → 1번 → 2번.
